{
  "name": "playwright-sdet-project",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:smoke": "playwright test --grep @smoke",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.11.0",
    "@playwright/test": "^1.49.0",
    "http-server": "^14.1.1",
    "typescript": "^5.6.0"
  },
  "dependencies": {
    "tsx": "^4.21.0"
  }
}
